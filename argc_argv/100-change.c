#include<stdio.h>


int main(int argc char *argv[])
{   int number_coins = 0;
	int coins[4] = {25, 10, 5, 1};
	int i;

	given = atoi(char argv[1]);
	if (argv != 1)
		printf("Error\n");
	if (given <= 0)
		printf("%d\n", 0)
	while (given > 0)
		{
			if (given / coins[i])
				++coins[i];
		}
	
	
}
