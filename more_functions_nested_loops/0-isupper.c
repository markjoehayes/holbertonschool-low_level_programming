#include "main.h"

/**
 * 0_isupper: checks for uppercase character
 * @c: character be tested
 * Return: 1 for uppercase, 0 otherwise
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);
	else
		return (0);
	return (0);
}

